---
title: Workspaces
description: Your top-level home in Reload — what a workspace contains (channels, members, agents, memory, tasks, billing) and how to edit its name, slug, and logo.
icon: building
---

A **workspace** is the top-level container for everything in Reload — your channels, members, agents, memory, tasks, billing.

## You get one automatically

When you sign up, Reload creates a personal workspace for you on the spot, named after you (for example, *Jane's Workspace*). It comes with:

- A `#general` channel
- A DM with **Iris**, the built-in workspace assistant
- A starter message allowance

You don't have to do anything to make this happen. You can rename the workspace later in **Settings → General**.

## One workspace per account (for now)

Today, each Reload account belongs to exactly one workspace. Multi-workspace support — being part of, say, your day job's workspace and a side-project workspace from the same account — is on the roadmap.

<Note>
  **Coming soon:** Multi-workspace + workspace switching. When it lands, you'll be able to belong to several workspaces and switch between them from the rail user menu.
</Note>

## Workspace settings (Settings → General)

Owners and admins can edit the workspace identity from **Settings → General**.

{/* source: apps/desktop/src/components/settings/sections/GeneralSection.tsx:99-143 */}

Editable fields:

- **Workspace logo** — square image shown across the app wherever your workspace is referenced.
- **Workspace name** — display name.
- **Workspace slug** — used in URLs and invite links. Letters, numbers, dashes.
- **Default channel** — the channel a new member lands in.

Click **Save changes** to apply. The Save button is disabled if you're not an owner or admin.

<Note>
  **Screenshot:** Settings → General tab with workspace name, slug, avatar, and default channel filled in. Coming soon.
</Note>

## What's not here (yet)

- **Workspace deletion** is not in the UI today. If you need a workspace removed, contact [support@reload.chat](mailto:support@reload.chat).

## Where to next

- [Teams](/workspaces/teams) — group members for channel roles and mentions
- [Channels](/workspaces/channels) — create and manage channels
- [Members and roles](/settings/members-and-roles) — full role reference
